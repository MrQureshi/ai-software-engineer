# CODE ANALYSIS

**Relevant files**

| Path | What it currently contains |
|------|----------------------------|
| `README.md` | Main project documentation. It already has an **Output** section that briefly mentions the `reports/` directory and a **Project structure** section that lists the source code layout. |
| `reports/01-plan` | Holds implementation‑plan markdown files generated by the **Planner** node. |
| `reports/02-codeAnalysis` | Holds code‑analysis markdown reports generated by the **Code Analyst** node. |
| `reports/03-implementation` | Holds the implementation‑plan markdown files (the “plan” folder is duplicated here – this is the folder used by the current code). |

**Existing implementation / patterns**

- The README follows a simple markdown layout with top‑level headings (`## How it works`, `## Prerequisites`, `## Setup`, `## Usage`, `## Output`, `## Project structure`, `## Tech stack`).
- The **Output** section already points readers to the `reports/` directory, but it does not describe the internal folder hierarchy or naming conventions.
- No Table of Contents is present, so adding a new heading will not break any existing links.

**Dependencies / files that may be affected**

- Only `README.md` will be edited. No source code changes are required.
- If a Table of Contents is later added, the new heading should be referenced there, but that is optional for this change.

**Recommended approach (ground‑truth steps)**

1. **Open `README.md`** – we already have its contents (see above).  
2. **Choose insertion point** – the most logical place is **right after the existing “## Output” section** (or after “## Project structure”) so readers see the folder description where they are already learning where reports are saved.  
3. **Draft a new heading**: `## Reports Folder`.  
4. **Write the description** – include the following points, mirroring the project’s terminology and folder names:
   - **Purpose** – where the CLI writes all generated markdown artifacts.
   - **Sub‑folders** – explain the three numbered directories:
     - `01‑plan` – implementation‑plan files (e.g., `implementation-plan-of‑<slug>.md`).
     - `02‑codeAnalysis` – code‑analysis reports (e.g., `code‑analysis‑of‑<slug>.md`).
     - `03‑implementation` – final implementation‑plan files (or any other artifacts the agent may produce).
   - **Naming convention** – files are slugified from the user request and saved with a `.md` extension.
   - **Generation flow** – the **Planner** node writes to `01‑plan`, the **Code Analyst** node writes to `02‑codeAnalysis`, and the final implementation plan is stored in `03‑implementation`. Mention that the files are created automatically when the CLI runs (`npm start`).
   - **How to view** – simply open the markdown files in any editor or render them with the built‑in terminal markdown renderer (`marked-terminal`).
5. **Insert the new section** – add the markdown after the “## Output” block (or after “## Project structure”). Keep the same indentation and line‑break style as the rest of the file.
6. **(Optional) Update a Table of Contents** – if the project later adds a TOC, add a bullet linking to `## Reports Folder`. For now, no TOC exists, so this step can be skipped.
7. **Save the file** – ensure the file ends with a newline.
8. **Run any markdown linting** (if the repo includes a linter) to confirm no style violations.
9. **Commit the change** with a clear message, e.g.:

   ```
   docs: add detailed description of reports folder to README
   ```

10. **Push** the commit and open a PR (if the workflow requires it). The updated README will now render a dedicated “Reports Folder” section on the repository’s main page.

**Resulting snippet (what will be added to `README.md`)**

```markdown
## Reports Folder

All artifacts generated by the CLI are stored under the top‑level `reports/` directory.

```
reports/
  01-plan/            # Implementation plans produced by the Planner node
  02-codeAnalysis/    # Code‑analysis reports produced by the Code Analyst node
  03-implementation/  # Final implementation‑plan files (or other output artifacts)
```

- **File naming** – each markdown file is named after the user request, slugified and suffixed with `.md` (e.g., `implementation-plan-of-create‑user‑api.md`).
- **Generation flow** – when you run `npm start`:
  1. The **Planner** node writes its numbered plan to `reports/01-plan/`.
  2. The **Code Analyst** node writes its analysis to `reports/02-codeAnalysis/`.
  3. The final implementation plan is saved in `reports/03-implementation/`.
- **Viewing the reports** – open any of the `.md` files in your favorite editor or view them directly in the terminal; the CLI already renders them with `marked-terminal` for a pretty output.

These reports give you a complete audit trail of the agent’s reasoning and the concrete steps it proposes for your feature request. 
```

By following the steps above you’ll have a clear, project‑consistent description of the `reports/` folder in the README, satisfying the user’s request without touching any functional code.
