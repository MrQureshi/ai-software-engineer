GROQ_API_KEY=

# Optional — LangSmith tracing/evaluation (see docs/20-langsmith-spec.md).
# Leave unset to run exactly as before; nothing else changes if these
# are absent. LANGCHAIN_* aliases (LANGCHAIN_TRACING_V2, LANGCHAIN_API_KEY,
# LANGCHAIN_PROJECT) also work, but the LANGSMITH_* names below are current.
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=
# LANGSMITH_PROJECT=ai-software-engineer


